---
phase: quick
plan: 260518
type: execute
wave: 1
depends_on: []
files_modified:
  - flake.nix
  - flake.lock
  - ops/hms-refresh.sh
  - modules/desktop-entries.nix
  - .gitignore
  - zsh-extra.sh
  - result
  - nvidia/version
  - tests/hms-aliases.sh
autonomous: true
requirements:
  - hms-dirty-warning-cleanup
must_haves:
  truths:
    - "hms still evaluates and switches the current Home Manager configuration"
    - "Codex Desktop input no longer creates an extra nixpkgs lock node"
    - "generated nvidia/version no longer carries trailing whitespace"
    - "tracked generated symlinks are removed from git tracking"
    - "Home Manager activation does not recreate profile desktop symlinks over managed desktop entries"
  artifacts:
    - path: "flake.nix"
      provides: "codex-desktop-linux follows repo nixpkgs"
      contains: "inputs.nixpkgs.follows"
    - path: "ops/hms-refresh.sh"
      provides: "normalized nvidia/version refresh"
      contains: "strip trailing whitespace"
    - path: "modules/desktop-entries.nix"
      provides: "managed desktop entries are not overwritten by profile desktop links"
      contains: "managedDesktopFiles"
    - path: ".gitignore"
      provides: "ignore generated zsh-extra.sh symlink"
      contains: "zsh-extra.sh"
  key_links:
    - from: "modules/desktop-entries.nix"
      to: "nixgl-apps.nix"
      via: "config.local.nixgl.desktopEntries"
      pattern: "builtins.attrNames config.local.nixgl.desktopEntries"
---

<objective>
Clean up the concrete issues found after the successful `hms` run without rolling back the currently active package baseline.

The warning set is mostly hygiene, but two items need code changes:

1. `codex-desktop-linux` should follow the repository nixpkgs so adding/updating the desktop input does not introduce an extra lockfile nixpkgs node.
2. The desktop refresh activation must not overwrite Home Manager managed desktop entries with package-profile desktop symlinks, which caused repeated "does not link into a Home Manager generation" messages.

Also normalize generated metadata and stop tracking the generated `zsh-extra.sh` symlink.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: tighten Codex Desktop flake input wiring</name>
  <files>flake.nix, flake.lock</files>
  <action>
Make `codex-desktop-linux` follow the root `nixpkgs`, and regenerate/repair the lockfile so there is only one repository nixpkgs node for the root configuration.
  </action>
  <verify>
    <automated>nix flake metadata --json . | jq '.locks.nodes.root.inputs'</automated>
  </verify>
  <done>The root config still evaluates and the Codex input no longer owns a separate nixpkgs node.</done>
</task>

<task type="auto">
  <name>Task 2: normalize generated NVIDIA metadata</name>
  <files>ops/hms-refresh.sh, nvidia/version</files>
  <action>
Update the refresh script so `/proc/driver/nvidia/version` is copied through a trailing-whitespace normalizer. Clean the current checked-out `nvidia/version`.
  </action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <done>`git diff --check` no longer reports trailing whitespace in `nvidia/version`.</done>
</task>

<task type="auto">
  <name>Task 3: stop profile desktop links from replacing managed entries</name>
  <files>modules/desktop-entries.nix</files>
  <action>
Teach the desktop refresh activation to skip profile desktop files whose basename is managed by `xdg.desktopEntries`. Remove the current stale profile symlink before switching so Home Manager can recreate the generation link.
  </action>
  <verify>
    <automated>readlink /home/mingshi/.local/share/applications/lenovo-legion-gui-pkexec.desktop</automated>
  </verify>
  <done>The Lenovo desktop entry links into a Home Manager generation, not directly into `~/.nix-profile`.</done>
</task>

<task type="auto">
  <name>Task 4: remove generated symlinks from git tracking</name>
  <files>.gitignore, zsh-extra.sh, result</files>
  <action>
Ignore `zsh-extra.sh` and `result`, then remove both from the git index while leaving the live symlinks on disk.
  </action>
  <verify>
    <automated>git status --short -- zsh-extra.sh result .gitignore</automated>
  </verify>
  <done>`zsh-extra.sh` and `result` are represented as tracked deletions plus `.gitignore` additions, and the local symlinks remain ignored.</done>
</task>

<task type="auto">
  <name>Task 5: verify hms-facing behavior</name>
  <files>tests/hms-aliases.sh</files>
  <action>
Keep the existing alias test runnable and execute it through bash. Build/evaluate the activation package, then run Home Manager switch once after removing the stale desktop symlink.
  </action>
  <verify>
    <automated>bash tests/hms-aliases.sh</automated>
    <automated>nix eval --raw .#homeConfigurations.mingshi.activationPackage.drvPath</automated>
    <automated>nix run .#home-manager -- switch --flake .</automated>
  </verify>
  <done>Tests and switch complete without the previously identified concrete hygiene failures.</done>
</task>

</tasks>

<success_criteria>
- `git diff --check` exits 0
- `bash tests/hms-aliases.sh` exits 0
- activation package evaluates
- Home Manager switch completes
- managed desktop entry no longer points at `~/.nix-profile/share/applications/lenovo-legion-gui-pkexec.desktop`
</success_criteria>
